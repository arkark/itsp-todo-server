version: "3.7"

services:
  postgresql:
    image: postgres:11.3
    environment:
      POSTGRES_USER: username
      POSTGRES_PASSWORD: password
    ports:
      - 5432:5432
    volumes:
      - ./db/data:/var/lib/postgresql/data

